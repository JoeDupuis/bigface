# Bigface

Video calling app built with vanilla Rails and WebRTC.

## Setup

```bash
bin/setup
bin/dev
```

## Structure

- `app/` - Rails application
- `android/` - Android app (Hotwire Native)
- `ios/` - iOS app (Hotwire Native) - planned
